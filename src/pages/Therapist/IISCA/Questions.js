import React from 'react'
import { StyleSheet, Text, View } from 'react-native'

const Questions = (props) => {
  const {iiscaId}=props
  return (
    <View style={{flex: 1}}>
      <Text>parent questions</Text>
    </View>
  )
}

export default Questions

const styles = StyleSheet.create({})
